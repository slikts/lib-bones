{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.16.4",
    "typescript": "^3.5.3"
  },
  "scripts": {
    "sync": "rsync -av --exclude='*.ts' src/ packages",
    "bootstrap": "lerna bootstrap --hoist",
    "pretest": "eslint packages",
    "test": "jest",
    "clean": "git clean -d -X -f packages",
    "build": "tsc -b"
  }
}
